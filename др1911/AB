#include <stdio.h>
#define SQUARE_SUM(A, B) ((A + B) * (A + B))

int main() {
    int a = 3, b = 4;
    int result = SQUARE_SUM(a, b);
    printf("Result: %d", result);
    return 0;
}

//gcc -E program.c -o preprocessed.c
